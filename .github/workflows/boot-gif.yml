name: Build and capture boot preview

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'src/**'
      - 'include/**'
      - 'user/**'
      - 'initramfs/**'
      - 'build/linker.ld'
      - 'Makefile'
      - 'scripts/**'
      - '.github/workflows/boot-gif.yml'
  push:
    paths:
      - 'src/**'
      - 'include/**'
      - 'user/**'
      - 'initramfs/**'
      - 'build/linker.ld'
      - 'Makefile'
      - 'scripts/**'
      - '.github/workflows/boot-gif.yml'

permissions:
  contents: read

jobs:
  boot-preview:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential nasm gcc-multilib qemu-system-x86 python3-pil

      - name: Build kernel
        run: make

      - name: Capture boot log
        run: |
          mkdir -p build/artifacts
          make qemu | tee build/artifacts/boot.log

      - name: Render boot GIF
        run: python3 scripts/render_boot_gif.py build/artifacts/boot.log build/artifacts/boot.gif

      - name: Upload boot artifacts
        uses: actions/upload-artifact@v4
        with:
          name: boot-preview
          path: |
            build/artifacts/boot.log
            build/artifacts/boot.gif
